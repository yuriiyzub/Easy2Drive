# 🚗 Easy2Drive - Система Тестування

## 📊 Архітектура "Гібридний підхід"

### ✅ Переваги реалізованого рішення:

**Питання → JSON файли (статичні дані)**
- Легко редагувати без бази даних
- Версіонування через Git
- Швидке завантаження
- Не потрібна складна міграція для оновлення питань

**Результати → База даних SQLite (динамічні дані)**
- Надійне зберігання результатів користувачів
- Швидкі запити для статистики
- Можливість аналітики
- Легко мігрувати на PostgreSQL для продакшену

---

## 📁 Структура проекту

```
Easy2Drive/
├── data/                              # JSON дані з питаннями
│   ├── categories.json                # Список всіх категорій
│   └── questions/                     # Питання по категоріях
│       ├── traffic-signs.json         # Дорожні знаки (10 питань)
│       ├── traffic-rules.json         # ПДР (10 питань)
│       ├── priority.json              # Пріоритет (10 питань)
│       ├── road-marking.json          # Розмітка (плануються)
│       ├── first-aid.json             # Перша допомога (плануються)
│       ├── traffic-lights.json        # Світлофори (плануються)
│       ├── parking.json               # Парковка (плануються)
│       └── safety.json                # Безпека (плануються)
│
├── prisma/                            # База даних
│   ├── schema.prisma                  # Схема БД
│   ├── dev.db                         # SQLite база (не в git)
│   └── migrations/                    # Міграції БД
│
├── lib/                               # Утиліти
│   ├── prisma.js                      # Prisma client
│   └── questions.js                   # Робота з питаннями
│
├── pages/
│   ├── categories.js                  # Список категорій ✅
│   ├── quiz/
│   │   ├── [categoryId].js           # Проходження тесту
│   │   └── result.js                  # Результати тесту
│   ├── history.js                     # Історія тестів
│   └── api/                           # API endpoints
│       ├── categories.js              # GET категорії ✅
│       ├── questions/
│       │   └── [categoryId].js       # GET питання категорії ✅
│       ├── quiz/
│       │   ├── create.js             # POST створити тест ✅
│       │   └── submit.js             # POST відправити результат ✅
│       └── results/
│           └── [userId].js           # GET результати користувача ✅
```

---

## 🎯 Категорії тестів

| ID | Назва | Іконка | Питань | Складність |
|----|-------|--------|---------|------------|
| `traffic-signs` | Дорожні знаки | 🚦 | 10 | Medium |
| `traffic-rules` | Правила ПДР | 🚗 | 10 | Medium |
| `priority` | Пріоритет проїзду | 🔀 | 10 | Hard |
| `road-marking` | Дорожня розмітка | 〰️ | 8 | Easy |
| `first-aid` | Перша допомога | ⚕️ | 8 | Medium |
| `traffic-lights` | Світлофори | 🚥 | 8 | Easy |
| `parking` | Зупинка та стоянка | 🅿️ | 8 | Medium |
| `safety` | Безпека руху | ⚠️ | 10 | Hard |

---

## 📝 Структура даних

### Категорія (JSON)
```json
{
  "id": "traffic-signs",
  "name": "Дорожні знаки",
  "nameEn": "Traffic Signs",
  "description": "Попереджувальні, заборонні, інформаційні знаки",
  "icon": "🚦",
  "color": "bg-blue-500",
  "totalQuestions": 10,
  "difficulty": "medium",
  "order": 1
}
```

### Питання (JSON)
```json
{
  "id": "ts_001",
  "categoryId": "traffic-signs",
  "question": "Що означає знак 'STOP'?",
  "image": null,
  "options": [
    "Рух без зупинки заборонено",
    "Зупинка заборонена",
    "Стоянка заборонена",
    "Обмеження швидкості"
  ],
  "correctAnswer": 0,
  "explanation": "Знак 'Рух без зупинки заборонено' вимагає обов'язкової зупинки...",
  "difficulty": "easy",
  "points": 1,
  "tags": ["знаки", "заборонні", "зупинка"]
}
```

### Результат тесту (БД - Prisma)
```typescript
model QuizResult {
  id              String   @id @default(cuid())
  userId          String
  categoryId      String
  quizType        String   // 'category', 'mixed', 'exam', 'mistakes'
  answers         String   // JSON
  score           Int
  totalQuestions  Int
  correctAnswers  Int
  percentage      Float
  timeSpent       Int
  passed          Boolean
  completedAt     DateTime
}
```

---

## 🔌 API Endpoints

### GET `/api/categories`
Отримати всі категорії тестів

**Відповідь:**
```json
[
  {
    "id": "traffic-signs",
    "name": "Дорожні знаки",
    "totalQuestions": 10,
    ...
  }
]
```

### GET `/api/questions/[categoryId]`
Отримати всі питання категорії

**Параметри:** `categoryId` - ID категорії

**Відповідь:**
```json
{
  "category": {...},
  "questions": [...],
  "total": 10
}
```

### POST `/api/quiz/create`
Створити новий тест

**Тіло запиту:**
```json
{
  "type": "category",      // 'category', 'mixed', 'exam'
  "categoryId": "traffic-signs",
  "count": 10,
  "difficulty": null       // 'easy', 'medium', 'hard' or null
}
```

**Відповідь:**
```json
{
  "questions": [...],      // Без правильних відповідей
  "total": 10
}
```

### POST `/api/quiz/submit`
Відправити результат тесту

**Тіло запиту:**
```json
{
  "categoryId": "traffic-signs",
  "quizType": "category",
  "answers": [
    {
      "selectedAnswer": 0,
      "timeSpent": 15
    }
  ],
  "questionIds": ["ts_001", "ts_002"],
  "timeSpent": 300
}
```

**Відповідь:**
```json
{
  "resultId": "clxxx...",
  "correctAnswers": 8,
  "totalQuestions": 10,
  "score": 8,
  "percentage": 80,
  "passed": true,
  "answers": [...]
}
```

### GET `/api/results/[userId]`
Отримати результати користувача

**Відповідь:**
```json
{
  "results": [...],
  "statistics": {
    "totalTests": 15,
    "passedTests": 12,
    "averageScore": 82.5,
    "categoryStats": {...}
  }
}
```

---

## 🎮 Типи тестів

### 1. Тест по категорії (`category`)
- Питання тільки з однієї категорії
- Кількість питань: 10
- URL: `/quiz/traffic-signs`

### 2. Змішаний тест (`mixed`)
- Випадкові питання з усіх категорій
- Кількість питань: вибирає користувач
- URL: `/quiz/mixed`

### 3. Пробний іспит (`exam`)
- 20 питань з різних категорій
- Симуляція реального іспиту
- Обмеження часу: 20 хвилин
- Прохідний бал: 70%
- URL: `/quiz/exam`

### 4. Робота над помилками (`mistakes`)
- Питання, на які раніше відповіли неправильно
- Персональний для кожного користувача
- URL: `/quiz/mistakes`

---

## 📊 Система оцінювання

- **Прохідний бал:** 70% правильних відповідей
- **Бали за питання:**
  - Easy: 1 бал
  - Medium: 1 бал
  - Hard: 2 бали

**Статуси:**
- ✅ **Passed** (Здано) - ≥ 70%
- ❌ **Failed** (Не здано) - < 70%

---

## 🚀 Як додати нові питання

### 1. Додати в існуючу категорію

Відредагуйте файл `/data/questions/[category-id].json`:

```json
{
  "id": "ts_011",
  "categoryId": "traffic-signs",
  "question": "Ваше питання?",
  "image": null,
  "options": ["Варіант 1", "Варіант 2", "Варіант 3", "Варіант 4"],
  "correctAnswer": 0,
  "explanation": "Пояснення правильної відповіді",
  "difficulty": "medium",
  "points": 1,
  "tags": ["тег1", "тег2"]
}
```

### 2. Створити нову категорію

1. Додайте категорію в `/data/categories.json`
2. Створіть файл `/data/questions/[new-category-id].json`
3. Додайте питання в новий файл

---

## 🗄️ База даних

### Таблиці:

1. **User** - Користувачі з Google OAuth
2. **UserProfile** - Додаткові дані профілю
3. **QuizResult** - Результати тестів
4. **MistakeQuestion** - Помилкові відповіді для роботи над помилками

### Команди Prisma:

```bash
# Створити міграцію
npx prisma migrate dev --name description

# Застосувати міграції
npx prisma migrate deploy

# Відкрити Prisma Studio (UI для БД)
npx prisma studio

# Згенерувати Prisma Client
npx prisma generate
```

---

## 🔄 Міграція на PostgreSQL

Коли буде потрібно перейти на PostgreSQL:

1. **Оновіть `prisma/schema.prisma`:**
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

2. **Оновіть `.env.local`:**
```env
DATABASE_URL="postgresql://user:password@localhost:5432/easy2drive"
```

3. **Запустіть міграції:**
```bash
npx prisma migrate dev
```

---

## 📈 Статистика користувача

Система відстежує:
- ✅ Загальна кількість тестів
- ✅ Здані/не здані тести
- ✅ Середній бал
- ✅ Статистика по категоріях
- ✅ Помилкові питання
- ✅ Час виконання
- ✅ Історія результатів

---

## ⚡ Наступні кроки

- [ ] Додати більше питань для всіх категорій
- [ ] Додати зображення для питань
- [ ] Реалізувати сторінку проходження тесту
- [ ] Додати таймер для іспиту
- [ ] Створити сторінку історії тестів
- [ ] Додати графіки прогресу
- [ ] Реалізувати систему досягнень
- [ ] Експорт результатів в PDF

---

## 🎨 UI компоненти

- ✅ Сторінка категорій з красивими картками
- ✅ Градієнти та анімації
- ✅ Адаптивний дизайн
- ✅ Іконки для кожної категорії
- ✅ Кольорові мітки складності

---

**Система повністю готова до використання!** 🚀

Питання зберігаються в JSON, результати в БД - найкраще рішення для вашого проекту!



