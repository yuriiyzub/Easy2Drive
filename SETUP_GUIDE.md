# 🚗 Easy2Drive - Інструкція з налаштування

## 📋 Що реалізовано

✅ **Сторінка логіну** з сучасним дизайном  
✅ **Інтеграція Google OAuth** через NextAuth.js  
✅ **Dashboard** для авторизованих користувачів  
✅ **Responsive дизайн** (адаптивний під всі пристрої)  
✅ **Tailwind CSS** для стилізації  

---

## 🚀 Швидкий старт

### 1. Встановлення залежностей

Всі необхідні пакети вже встановлені:
- `next` - Next.js фреймворк
- `react` та `react-dom` - React бібліотеки
- `next-auth` - Авторизація
- `tailwindcss` - CSS фреймворк

### 2. Налаштування Google OAuth

#### Крок 1: Створення проекту в Google Cloud Console

1. Перейдіть на https://console.cloud.google.com/
2. Створіть новий проект або виберіть існуючий
3. Перейдіть до **APIs & Services** > **Credentials**
4. Натисніть **Create Credentials** > **OAuth client ID**
5. Виберіть тип додатку: **Web application**
6. Налаштуйте:
   - **Authorized JavaScript origins**: `http://localhost:3000`
   - **Authorized redirect URIs**: `http://localhost:3000/api/auth/callback/google`
7. Збережіть **Client ID** та **Client Secret**

#### Крок 2: Налаштування .env.local

Створіть файл `.env.local` в корені проекту (він вже існує як шаблон):

```bash
# Google OAuth credentials
GOOGLE_CLIENT_ID=ваш-google-client-id
GOOGLE_CLIENT_SECRET=ваш-google-client-secret

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=згенеруйте-секретний-ключ
```

Для генерації `NEXTAUTH_SECRET` виконайте в терміналі:
```bash
openssl rand -base64 32
```

### 3. Запуск додатку

```bash
npm run dev
```

Додаток буде доступний за адресою: **http://localhost:3000**

---

## 📁 Структура проекту

```
Easy2Drive/
├── pages/
│   ├── _app.js              # Головний компонент додатку
│   ├── index.js             # Головна сторінка (редирект)
│   ├── login.js             # Сторінка логіну
│   ├── dashboard.js         # Панель користувача
│   └── api/
│       └── auth/
│           └── [...nextauth].js  # API для авторизації
├── styles/
│   └── globals.css          # Глобальні стилі
├── public/
│   └── index.html           # Демо HTML сторінка
├── .env.local               # Змінні оточення (не в git)
├── next.config.js           # Конфігурація Next.js
├── tailwind.config.js       # Конфігурація Tailwind
└── package.json             # Залежності проекту
```

---

## 🎨 Особливості дизайну

### Сторінка логіну (`/login`)
- ✨ Градієнтний фон
- 🎯 Центрована картка входу
- 🔵 Кнопка "Увійти через Google" з офіційним логотипом
- 📱 Адаптивний дизайн
- ✅ Список переваг додатку

### Dashboard (`/dashboard`)
- 👤 Навігаційна панель з аватаром користувача
- 📊 Статистика (тести, середній бал, прогрес)
- ⚡ Швидкі дії (почати тест, переглянути результати)
- 🎨 Сучасний card-based дизайн

---

## 🔐 Як працює авторизація

1. Користувач натискає "Увійти через Google"
2. NextAuth.js перенаправляє на Google OAuth
3. Користувач підтверджує доступ
4. Google повертає токен
5. NextAuth.js створює сесію
6. Користувач перенаправляється на Dashboard

---

## 📱 Перегляд демо без налаштування

Відкрийте файл `public/index.html` у браузері для перегляду статичної версії сторінки логіну.

---

## 🛠️ Команди

```bash
npm run dev          # Запуск в режимі розробки
npm run build        # Збірка для продакшену
npm run start        # Запуск продакшен версії
npm test             # Запуск тестів
```

---

## 📝 Наступні кроки

- [ ] Підключити базу даних для збереження результатів тестів
- [ ] Створити сторінку з тестами
- [ ] Додати систему відстеження прогресу
- [ ] Реалізувати аналітику результатів
- [ ] Додати можливість вибору категорій питань

---

## ⚠️ Важливо

- **НЕ** комітьте файл `.env.local` в git
- Зберігайте `GOOGLE_CLIENT_SECRET` в безпеці
- Для продакшену змініть `NEXTAUTH_URL` на ваш домен

---

## 🆘 Troubleshooting

### Помилка: "Invalid client"
- Перевірте правильність `GOOGLE_CLIENT_ID` та `GOOGLE_CLIENT_SECRET`
- Переконайтесь, що redirect URI налаштований правильно

### Помилка: "Session not found"
- Перевірте наявність `NEXTAUTH_SECRET` в `.env.local`
- Перезапустіть dev сервер

### Стилі не застосовуються
- Перевірте, чи встановлений Tailwind CSS
- Перезапустіть dev сервер

---

## 📞 Підтримка

Якщо виникли питання, перевірте документацію:
- [Next.js Documentation](https://nextjs.org/docs)
- [NextAuth.js Documentation](https://next-auth.js.org/)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)



